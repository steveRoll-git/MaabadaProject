#ifndef MACROPROCESSOR_H
#define MACROPROCESSOR_H

/* Preprocesses the file at the input path, and outputs the result to the output file. */
/* Returns 1 if no errors occurred, and 0 otherwise. */
int preprocess(char *input_file_path, char *output_file_path);


#endif

